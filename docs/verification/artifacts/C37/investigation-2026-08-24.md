# C37 成因调查 — 2026-08-24(七个样本)

材料来自 C7 刚建的读路径 `GET /calls/{callId}/queue-events`。
FreeSWITCH 自己的日志只回溯到 08-23,七通(08-20…08-22)已轮转掉;
用的是 `queue_events`、`agent_state_logs` 与应用日志。

## 一、已确证

### 1. 七通全部是同一部话机 —— wei / 1008,**浏览器话机**

```
01a01e7b  08-20 17:23  22 次  → 807b2164 (wei)
01a021f5  08-21 09:35  13 次  → 807b2164
01a02299  08-21 12:35  23 次  → 807b2164
01a022da  08-21 13:45  23 次  → 807b2164
01a02729  08-22 09:50  33 次  → 807b2164
01a0272c  08-22 09:54  24 次  → 807b2164
01a0272d  08-22 09:56  10 次  → 807b2164
```

**ben 的原生软电话(1002)一次都没有。** 七比零。
这把嫌疑从 mod_callcenter 与应用移到 **web-sip-phone / SIP.js 那一侧**。

### 2. 节奏是成簇的,不是均匀的

```
call          queue        一秒内次数   簇的时刻
01a02729      support-zh      10        09:51:24
01a02729      support-zh      10        09:52:25
01a02729      support-zh       9        09:53:26
01a0272c      support-en      10        09:55:28
01a0272c      support-en      11        09:56:29
01a02299      support-en      10        12:36:08
01a02299      support-en      10        12:37:09
```

**一秒内约 10 次,然后正好 61 秒后再来一簇**,直到主叫放弃。
两个队列节奏完全相同,所以**队列参数不是变量**。
立案时记的"三分钟 42 次、簇内约 70ms"是同一现象的另一种描述,但**结构是簇**。

### 3. 触发条件:一次**被主叫放弃打断**的派单

```
09:49:23  call A  JOINED     support-en
09:49:39  call A  OFFERED    wei        ← wei 刚从 BREAK 转 READY(09:49:39)
09:50:02  call A  ABANDONED  (39 秒)    ← 主叫在派单未解决时挂断
09:50:23  call B  JOINED + OFFERED wei
09:51:24  call B  OFFERED ×10           ← 忙等开始
```

派单**不是被 RONA 超时结束的**,是主叫中途挂断、mod_callcenter 取消了这次 origination。
立案时写的"一次被取消的派单"是对的,这里补上了**取消的原因**。

### 4. 一旦卡住,**跨通话持续**

```
storm         storm_start        前一条队列移动
01a021f5      08-21 09:35:20     01a01ed3 ABANDONED
01a022da      08-21 13:45:54     01a022d9 ABANDONED @13:45:06
01a02729      08-22 09:50:23     01a02728 ABANDONED @09:50:02
01a0272c      08-22 09:54:28     01a02729 ABANDONED @09:53:51   ← 上一场风暴刚结束
01a0272d      08-22 09:54:52     01a0272c OFFERED   @09:54:28   ← 上一场风暴**还在进行**
```

四通紧跟在前一通被放弃之后,一通在**前一场风暴尚未结束时**就开始。
**话机不会在两通之间自愈** —— 是链式的,一次卡死污染此后整个窗口。

### 5. 应用在整场风暴里一个字都没记

`01a02729` 的三分半钟里,应用日志从 09:50:23 的 `sip bye sent` 直接跳到 09:53:51 的 CDR 告警。
派单事件被写进 `queue_events`,但**不打日志**。
运营盯着应用日志看一场风暴,看到的是**静默**。

### 6. 应用侧的坐席状态是**对的**

`agent_state_logs`:wei 09:49:39 由 NOT_READY(BREAK) 转 READY,直到 09:58:30 一直 READY。
不是"镜像漂移把一个不在的人标成可派" —— wei 确实可派,是话机不接。

## 二、新发现:W2.1 的对策**只解决了一半**

`cbf0391`(08-23)下发 `max_no_answer=2 / no_answer_delay_time=60 /
reject_delay_time=60 / busy_delay_time=60`。

`reject_delay_time` 从 0 改成 60,把**一秒十次**变成**一分钟一次** —— 安静了。
**但主叫仍然等不到人**:

**拒绝不是无应答**。mod_callcenter 的 `no_answer_count` 只被"振铃到超时"递增;
一部回 486 的话机永远不递增它,于是 **`max_no_answer=2` 永远不触发**,
坐席**永远不会被置为 On Break、永远不退出轮转**。
(现场 `callcenter_config agent list` 里 agent-wei 的 `no_answer_count` 读作 0。
这是弱佐证 —— 需要一次确认实验,见下。)

所以对策改变的是**噪声频率**,不是**结果**:主叫照样一路听保持音到放弃。
**缺的那一半是**:连续拒绝 N 次的话机应当被当作不可用。

## 三、仍不知道

1. **61 秒这个周期从哪来。** 四个 agent 参数都是 08-23 才写的(`cbf0391`),
   七通发生时它们都是 mod_callcenter 的默认值(均为 0),**没有一个是 60**。
   队列的 `rona_delay_sec` 是 10、`tier_rule_wait_sec` 是 300,也都不是。**未解。**
2. **浏览器话机为什么回 486。** 这是最终成因,仍未查明。
   已知它与"派单被主叫放弃打断"相关,且不会自愈。

## 四、可复现的实验(C37 一直缺的就是这个)

链条给出了配方:

1. wei 的浏览器话机注册、READY;
2. 一通 AI 呼叫转人工进队列 → wei 的话机开始振铃;
3. **主叫在 wei 的话机还在响时挂断**(这一步是关键 —— 制造"被取消的派单");
4. 立刻打第二通进同一队列;
5. 观察 `GET /calls/{callId2}/queue-events` 的 OFFERED 次数。

**判据**:
- 第二通只被派 1–2 次 → 未复现,或 `reject_delay_time=60` 之外还有别的变化;
- 第二通被反复派、且间隔约 60 秒 → **复现成功**,且证实对策只压住了频率;
- 同时看 `callcenter_config agent list` 的 `no_answer_count` 是否仍为 0 → 确认"拒绝不计无应答"。

**对照组**:同一流程改用 ben 的原生软电话(1002)。若不复现,嫌疑就锁定在浏览器话机侧,
而那需要在 `web-sip-phone` 会话里处理(它是另一个仓库,改动走那条路)。

---

# 补录 · 根因确认与 486 实验(同日,12:00–12:10)

## 一、根因:web-sip-phone 的 RESET 出路只有一条

读 `web-sip-phone/src/offscreen/call-session.ts` 得到:

- `handleInvite`(:67-77)只在 `session === null && state === CallState.Idle` 时受理,否则回 486。
- 被取消的振铃:`handleSessionState` 收到 `Terminated`、`state === Ringing` → 映射 `CANCEL`
  → `session = null`、状态进入 **Ended**。
- **离开 Ended/Failed 只有两条路**:`setState` 挂的 `resetTimer`
  (`RESET_MS_ENDED=1000` / `RESET_MS_FAILED=3000`),或 `forceIdle()` ——
  而 `forceIdle()` **唯一调用方**是 `sip-runtime.ts:418` 的 `rebuildUa()`,即只有传输重建。

**那一个 `setTimeout` 没执行,话机就对之后每个 INVITE 回 486,直到传输重建。**
这解释了全部七次:只在浏览器话机、由被取消的振铃触发、跨通话不自愈、隔一阵子(重连后)又好。

**aicc 侧无法证实也无法排除的部分**:应用只记 `registrations reconciled`,
**不记单个话机的注册/注销**,所以对方提出的第二条路径(`terminate()` 清定时器不补跑 RESET,
配置变更的 stop→start 继承 Ended)在我们的日志里查不到佐证。这本身是个观测缺口。

## 二、web-sip-phone 的处置(对方会话答复,已修)

1. `handleInvite` 惰性补跑 RESET:`session === null` 且处于 Ended/Failed 时先回 Idle 再受理。
   **定时器降级为 UI 快路径,受理不再依赖任何后台定时器。**
2. `SipRuntime.start()` 开头调 `forceIdle()`,堵住 stop→start 继承路径。
3. 486 按状态拆分:真在通话/振铃的四态仍 486;**槽被一个已进终态的 session 占着 → 480**。
4. 那个 1s/3s 的合法 486 窗口**取消**,不是缩短。
5. 新增诊断字段 `overdueByMs` —— 正值即"定时器该跑而没跑"的硬证据。238/238 通过。

**他们驳回了我提的"压住 INVITE 等 1 秒",理由更好**:压住会重新引入一个
"必须按时醒来"的依赖,正是这次出事的那一类东西。

## 三、486 实验:交换机侧**没有**兜底

做法(无需人工):`originate {sip_auto_answer=true}user/1008 &park()` 占住 wei 的话机,
再把一个主叫送进 **support-en**(该队列只配了 wei)。

```
12:01:15.83  INVITE → wei
12:01:15.83  entering state [terminated][486]
12:01:15.83  Hangup … [CS_CONSUME_MEDIA] [USER_BUSY]
```

两次独立执行(12:01、12:05),**wei 全程 `status=Available` / `no_answer_count=0`**。

**结论:486 → USER_BUSY,不计入 `max_no_answer`,坐席不会被挪出轮转。**
对照:同日 11:31 的**无应答**一型,15 秒超时后 wei 立刻被置 `NOT_READY(SYSTEM)` 并停止派单。
**两条路径的兜底待遇完全不同。**

**未观察到的部分,如实记**:两次的合成主叫分别只待了 14 秒与 41 秒,
都不足 `busy_delay_time=60`,所以**没有看到第二次派单**。
"会不会一分钟一次无限重试"是**从参数推的,不是测出来的**。

## 四、C37 的现状

- **成因**:已在代码层确认(web-sip-phone),浏览器侧的冻结机制未证实,对方选择不证实 ——
  修复对机制不敏感,并留了 `overdueByMs` 让下一次现场自证。
- **修复**:在 web-sip-phone 一侧,已落地。
- **aicc 侧兜底**:**无应答有,486 没有**。若将来出现别的成因导致话机持续 486,
  交换机侧拦不住 —— 这是一个已知的、有意留下的缺口,记在此处。

---

# 再补录 · 我方的"迟到 Terminated"候选(不构成证据)

web-sip-phone 在收到我们"交换机侧没有兜底"的实验结果后,又自查出两处,其中一处对我们有直接后果:
**被丢弃的 session 迟到投递 `Terminated` 时,会对当时正在通话的另一路 apply CANCEL/BYE ——
等于挂断一通无关的电话。** 该竞态在这次改动之前就存在,已一并堵上。

在账本里找痕迹,特征是"已接通的电话里坐席腿远早于整通结束,且剩余时间用
`bot_sec / queue_wait_sec / ring_sec` 解释不掉":

```
   时间        call            ext   agent_sec  qwait  ring  bot  total  无法解释
08-23 16:42  01a02dc8-e391   1008        11      0     21    0    123      91
08-20 18:53  01a01ecd-eb26   1008        13     15      2    0    112      82
08-20 18:47  01a01ec8-91d0   1008        13     15      2    0     71      41
08-20 18:36  01a01ebe-ce9e   1008        26      0     14    0     65      25
```

**四条全部是 1008(wei 的浏览器话机)**,ben 的原生软电话零条 —— 与七次风暴同样的 7:0 形状;
三条挤在 08-20 18:36–18:53,而当天 17:23 刚发生过一次派单风暴。

**已排除的假阳**:`01a023ec-49fa` 的"无法解释 395 秒"是转接(1008→1007 两条坐席腿),另一半时间在同事腿上。

**⚠ 不称其为证据**:CDR 分不出①话机拆了腿 ②坐席自己挂断 ③主叫被重排队/走溢出,
账本里没有能区分它们的字段。**把这个形状当结论,正是这轮调查两边都在避免的那类错误。**
能定论的是 web-sip-phone 新加的诊断行
`session slot still occupied long after the call ended; discarding zombie`(带 `state`/`elapsedMs`);
它一旦在现场出现,我们可以拿同一时刻的 CDR 与 `queue_events` 对齐。

## 订正:前面记的"我方观测缺口"撤销

前文记"应用不记单个话机的注册/注销,所以对方的 stop→start 继承路径查不到佐证"——
对方指出该路径**完全在浏览器进程内、不产生任何注册/注销信令**,FreeSWITCH 侧本来就看不见。
**所以那不是我们的日志粒度不够,是那条路径不在可观测范围内**;再细的注册日志也查不到。
该条撤销。
