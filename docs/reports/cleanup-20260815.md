# Moved

The 2026-08-15 drift audit now lives with the design set it amended, as
[`docs/design/m4-cleanup-findings.md`](../design/m4-cleanup-findings.md) —
alongside `m0-findings.md`, which plays the same role for the M0 spike.

It is kept in one place rather than two so the findings and the design cannot
drift apart, which is what the audit itself was about.
